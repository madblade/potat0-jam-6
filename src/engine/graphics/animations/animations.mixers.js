/**
 * Holds logic for:
 * - walk / run (synchronized blend)
 * - jump
 * - crouch
 * - attack
 */

'use strict';

let AnimationMixers = {

    updateMixerAction(
        entity,
        entityId,
        deltaT
    )
    {
        // here update mixer.
    }

};

export { AnimationMixers };
